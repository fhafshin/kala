

namespace NodeJs{

    interface ProcessENV{


        APP_PORT:string;
        DB_PORT:number;
        DB_HOST:string;
        DB_PASSWORD:string;
        DB_USERNAME:string;
        DB_TYPE:string;
        DB_NAME:string;
    }
}